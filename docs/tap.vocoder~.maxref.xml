<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>

<c74object name="tap.vocoder~" module="msp" category="TapTools, MSP Filters, MSP Modifiers">

	<digest>
		Basic 24-band vocoder
	</digest>

	<description>
		The <o>tap.vocoder~</o> object is a basic 24-band channel vocoder. A bank of log-spaced
		bandpass filters analyses the <b>modulator</b> (left inlet, e.g. a voice); a per-band
		envelope follower tracks each band's amplitude and shapes the matching band of the
		<b>carrier</b> (right inlet, e.g. a synthesizer). The result imposes the modulator's
		spectral envelope onto the carrier.
	</description>


	<!--METADATA-->
	<metadatalist>
		<metadata name="author">74 Objects</metadata>
		<metadata name="tag">TapTools</metadata>
		<metadata name="tag">Audio</metadata>
		<metadata name="tag">Effect</metadata>
		<metadata name="tag">Envelope</metadata>
	</metadatalist>


	<!--INLETS-->
	<inletlist>
		<inlet id="0" type="signal">
			<digest>Modulator — the spectral-envelope source (e.g. a voice).</digest>
		</inlet>
		<inlet id="1" type="signal">
			<digest>Carrier — the signal to be shaped (e.g. a synthesizer).</digest>
		</inlet>
	</inletlist>


	<!--OUTLETS-->
	<outletlist>
		<outlet id="0" type="signal">
			<digest>The vocoded output.</digest>
		</outlet>
	</outletlist>


	<!--MESSAGES-->
	<methodlist>
		<method name="clear">
			<description>Reset all filter and envelope-follower state.</description>
		</method>
	</methodlist>


	<!--ATTRIBUTES-->
	<attributelist>
		<attribute name="q" get="1" set="1" type="float" size="1">
			<description>The Q factor (resonance) shared by all of the resonant bandpass filters
			(0.5-200, default 20). Higher values give narrower, more "robotic" bands. A number,
			not a symbol (the original object registered it as a symbol by mistake).</description>
		</attribute>
		<attribute name="response_interval" get="1" set="1" type="float" size="1">
			<description>The envelope-follower analysis period for the excitation stage, in
			milliseconds (0.1-1000, default 20). Shorter values track the modulator more sharply;
			longer values smooth it. A number, not a symbol (the original object registered it as
			a symbol by mistake).</description>
		</attribute>
		<attribute name="gain" get="1" set="1" type="float" size="1">
			<description>Linear makeup gain applied to the summed output (0-100, default 1).</description>
		</attribute>
		<attribute name="sibilance" get="1" set="1" type="float" size="1">
			<description>Sibilance amount (0..1). Blends a deterministic seeded noise source into the carrier path of the bands above ~4 kHz, so unvoiced consonants come through while the modulator gates them. At 0 the output is identical to the noise-free vocoder. Default 0.</description>
		</attribute>
		<attribute name="mix" get="1" set="1" type="float" size="1">
			<description>Wet/dry mix in percent (0..100), equal-power. The dry side is the carrier (the program material); 100 is fully vocoded, 0 passes the carrier through untouched. Default 100.</description>
		</attribute>
		<attribute name="seed" get="1" set="1" type="int" size="1">
			<description>Seed for the sibilance noise source (&gt;= 1). The noise is deterministic per seed; give mc. instances different seeds to decorrelate them. Default 1.</description>
		</attribute>
		<attribute name="bypass" get="1" set="1" type="int" size="1">
			<description>Pass the carrier through unprocessed. Default 0 (off).</description>
		</attribute>
		<attribute name="mute" get="1" set="1" type="int" size="1">
			<description>Silence the output. Default 0 (off).</description>
		</attribute>
	</attributelist>


	<!--SEEALSO-->
	<seealsolist>
		<seealso name="tap.svf~"/>
		<seealso name="tap.comb~"/>
		<seealso name="vocoder~"/>
	</seealsolist>

</c74object>
