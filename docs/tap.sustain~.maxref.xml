<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<?xml-stylesheet href="./_c74_ref.xsl" type="text/xsl"?>

<c74object name="tap.sustain~" module="msp" category="TapTools, MSP Sampling">

	<digest>
		Capture recent audio into seamless sustaining loops
	</digest>

	<description>
		The <o>tap.sustain~</o> object continuously records its signal input into a ring buffer.
		On a bang, the most recently recorded material is captured into a voice: trimmed to
		significant-power rising zero-crossings at both ends, then played back as a crossfaded
		forward/backward loop so it sustains without clicks. A bank of voices is allocated
		round-robin, so successive bangs stack overlapping loops — up to the voices count —
		before the oldest voice is recycled (round-robin doubles as an oldest-first
		voice-stealing policy). Each freshly captured voice swells in over the rise time, so a
		newly stacked loop grows in under the existing ones rather than clicking in. All
		sounding voices are summed to the output.
	</description>


	<!--METADATA-->
	<metadatalist>
		<metadata name="author">Timothy Place</metadata>
		<metadata name="tag">TapTools</metadata>
		<metadata name="tag">Audio</metadata>
		<metadata name="tag">Sampling</metadata>
	</metadatalist>


	<!--INLETS-->
	<inletlist>
		<inlet id="0" type="signal">
			<digest>Audio to record; bang captures a sustaining loop.</digest>
		</inlet>
	</inletlist>


	<!--OUTLETS-->
	<outletlist>
		<outlet id="0" type="signal">
			<digest>Sustaining loop output (all voices summed).</digest>
		</outlet>
	</outletlist>


	<!--MESSAGES-->
	<methodlist>
		<method name="bang">
			<arglist/>
			<description>Capture the most recently recorded audio into a sustaining loop,
			allocated into the next voice round-robin so it overlaps any voices already
			sounding. When all voices are in use, the oldest one is stolen.</description>
		</method>
		<method name="clear">
			<arglist/>
			<description>Erase all captured loops (every voice) and reset the round-robin
			allocation cursor.</description>
		</method>
	</methodlist>


	<!--ATTRIBUTES-->
	<attributelist>
		<attribute name="length" get="1" set="1" type="float" size="1">
			<description>Maximum length of the captured loop, in milliseconds (1 or higher;
			default 1000). The value is stored immediately, but the recording buffer re-sizes
			to match at the next DSP start (restart audio to apply it); until then captures
			keep using the previous length.</description>
		</attribute>
		<attribute name="voices" get="1" set="1" type="int" size="1">
			<description>Number of overlapping sustaining voices (1-5, default 5). Each bang
			captures into the next voice round-robin, so up to this many loops can stack
			before the oldest is recycled.</description>
		</attribute>
		<attribute name="fade" get="1" set="1" type="float" size="1">
			<description>Length of the equal-power crossfade across the loop points, in
			milliseconds (default 100).</description>
		</attribute>
		<attribute name="rise" get="1" set="1" type="float" size="1">
			<description>Time over which each newly captured voice grows to full power, in
			milliseconds (default 100). Applied independently per voice as a one-shot fade-in
			when it starts sustaining, so a stacked voice swells in under the others rather
			than clicking in.</description>
		</attribute>
	</attributelist>


	<!--SEEALSO-->
	<seealsolist>
		<seealso name="buffer~"/>
		<seealso name="record~"/>
		<seealso name="tap.buffer.record~"/>
	</seealsolist>


</c74object>
